{
  "entities": {
    "GalleryPhoto": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GalleryPhoto",
      "type": "object",
      "description": "Represents a photo in the gallery.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the gallery photo."
        },
        "url": {
          "type": "string",
          "description": "URL of the gallery photo.",
          "format": "uri"
        },
        "title": {
          "type": "string",
          "description": "Title or caption for the gallery photo."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the gallery photo."
        },
        "isAboutPhoto": {
          "type": "boolean",
          "description": "Flag indicating if this is the photo for the 'My Story' page."
        }
      },
      "required": [
        "id",
        "url"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/gallery_photos/{photoId}": {
      "schema": {
        "$ref": "#/entities/GalleryPhoto"
      },
      "description": "Stores all photos for the gallery and the about page."
    }
  }
}
